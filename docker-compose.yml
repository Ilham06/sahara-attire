services:
  sahara-attire:
    build: .
    container_name: sahara-attire
    restart: always
    ports:
      - "3007:3007"
    environment:
      - NODE_ENV=production
      - PORT=3007
      - NEXT_IMAGE_UNOPTIMIZED=true